# تعليمات تفعيل: خيار الواتساب/الإيميل + رقم الداشبورد

بعد ما تخلص الخطوات دي، هيبقى عندك:
- زرارين تحت كل فورم في الموقع: **إرسال عبر واتساب** و **إرسال عبر البريد الإلكتروني**.
- تاب جديد اسمه **الإعدادات** في الداشبورد تقدر منه تغيّر رقم الواتساب والإيميل الرسمي في أي وقت من غير ما تلمس الكود.

---

## 1) جدول قاعدة البيانات (خطوة واحدة، 5 دقايق)

1. افتح مشروعك في [supabase.com](https://supabase.com) → **SQL Editor** → **New query**.
2. افتح ملف `supabase-setup.sql` المرفق، انسخ كل محتواه، الصقه، ودوس **Run**.
3. ده بينشئ جدول `site_settings` (الرقم + الإيميل) وبيضبط صلاحيات `form_messages`.

---

## 2) حساب Resend (خدمة إرسال الإيميلات)

1. اعمل حساب مجاني على [resend.com](https://resend.com) (فيه باقة مجانية تكفي بداية).
2. من **Domains**، ضيف الدومين بتاع إيميلك الرسمي (مثلاً `yourdomain.com`) واتبع خطوات التحقق (إضافة سجلات DNS — TXT/CNAME عند مزوّد الدومين بتاعك).
   - لو عايز تجرب بسرعة قبل ما توثّق الدومين، ممكن تستخدم مؤقتًا `onboarding@resend.dev` كمرسل، بس الإيميلات وقتها ممكن تروح سبام عند بعض مزودين البريد.
3. من **API Keys** أنشئ مفتاح جديد وخزّنه، هتحتاجه في الخطوة الجاية.

---

## 3) نشر الـ Edge Function

محتاج [Supabase CLI](https://supabase.com/docs/guides/cli) مثبت عندك على جهازك.

```bash
# سجّل دخول
supabase login

# اربط المجلد بمشروعك (project-ref موجود في رابط الداشبورد بتاعك في supabase.com)
supabase link --project-ref xrxmchyaemzfsrpnyzsu

# انشر الفنكشن (الملف موجود في supabase/functions/send-order-email/index.ts)
supabase functions deploy send-order-email
```

بعدين حط الأسرار (secrets) اللي الفنكشن محتاجها:

```bash
supabase secrets set RESEND_API_KEY=re_xxxxxxxxxxxx
supabase secrets set NOTIFY_FROM_EMAIL=orders@yourdomain.com
```

> `SUPABASE_URL` و `SUPABASE_SERVICE_ROLE_KEY` متوفرين تلقائيًا جوه أي Edge Function، مش محتاج تحطهم بنفسك.

---

## 4) رفع ملفات الموقع والداشبورد

- ارفع مجلد الموقع المُعدّل (استبدل النسخة القديمة بالكامل، أو على الأقل: `index.html`, `assets/css/style.css`, `assets/js/script.js`, `assets/js/data-service.js`).
- ارفع مجلد `admin/` المُعدّل بنفس الطريقة (استبدل `dashboard.html`, `dashboard.js`, `dashboard.css`).

---

## 5) ضبط الرقم والإيميل من الداشبورد

1. ادخل داشبورد الأدمن → تاب **الإعدادات**.
2. اكتب رقم الواتساب بصيغة دولية بدون `+` أو مسافات، مثال: `966536760429`.
3. اكتب الإيميل الرسمي اللي عايز الطلبات توصله (لازم يكون على نفس الدومين اللي وثّقته في Resend، أو أي إيميل تاني تحب تستقبل عليه).
4. دوس **حفظ الإعدادات**.

من لحظة الحفظ:
- رقم الواتساب هيتحدث في كل الموقع (الفورم + أزرار الاتصال) تلقائي.
- زرار "إرسال عبر البريد الإلكتروني" هيظهر في الفورمات (كان مخفي لحد ما تحط إيميل).

---

## ملاحظات مهمة

- لو حصل خطأ وقت إرسال الإيميل (مثلاً الدومين لسه مش متوثّق في Resend)، الزائر هيشوف رسالة خطأ واضحة تقوله يجرب واتساب بدل ما الفورم "يعلّق" من غير رد.
- كل الطلبات (واتساب أو إيميل) بتتسجل في تاب "الرسائل" في الداشبورد برضو، وهتلاقي جنب كل رسالة علامة توضح جاية من واتساب ولا إيميل.
- لو عايز تغيّر الإيميل تاني أو الرقم بعدين، ارجع لتاب الإعدادات وغيّره وقت ما تحب — مفيش داعي ترفع كود تاني.
